<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>

        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--Main Linear Layout-->
    <style name="mainLinearLayout">
        <item name="android:background">@color/background</item>
        <item name="android:paddingBottom">@dimen/paddingbottom</item>
    </style>

    <!-- Titles -->
    <style name="title">
        <item name="android:paddingLeft">@dimen/padding</item>
        <item name="android:paddingRight">@dimen/padding</item>
        <item name="android:paddingTop">@dimen/padding</item>
        <item name="android:textColor">@color/textColor</item>
        <item name="android:textSize">@dimen/textTitleDescription</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Description -->
    <style name="description">
        <item name="android:padding">@dimen/padding</item>
        <item name="android:text">@string/description</item>
        <item name="android:textColor">@color/textColor</item>
        <item name="android:textSize">@dimen/textTitleDescription</item>
    </style>


    <!-- Contacts -->
    <style name="contacts">
        <item name="android:textColorLink">@color/textColor</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/textColor</item>
    </style>


    <!-- Icon Contacts -->
    <style name="iconContacts">
        <item name="android:layout_margin">8dp</item>
        <item name="android:scaleType">centerCrop</item>
        <item name="android:tint">@color/iconLime</item>
    </style>

</resources>
